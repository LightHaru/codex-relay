# Codex Relay 0.4.1 validation evidence — 2026-08-24

This record is intentionally sanitized. It contains no account IDs, full
identity fields, control token, credentials, prompt, Goal objective, workspace
path, rollout path, tool arguments or raw upstream error.

## Automated gates

Run from the repository root after the version and documentation update:

| Gate | Final exit code | Result |
| --- | ---: | --- |
| `npm ci --ignore-scripts` | 0 | 8 lockfile-resolved packages installed; 0 audit vulnerabilities |
| Focused `go test ./internal/state ./internal/mux ./internal/control` | 0 | Explainability, routing, state and control tests passed |
| Focused `node --test ui/test-windows-router-menu.cjs` | 0 | 26 Windows bridge/inspector tests passed |
| `npm run check` | 0 | Go test/vet, 29 Node tests, Python syntax, 24 Windows patch tests and shell syntax passed; one environment-optional patch test skipped |
| `npm run release:check` | 0 | `v0.4.1` metadata consistent |
| `git diff --check` | 0 | No whitespace errors |

The local Windows host reported `CGO_ENABLED=0` and no GCC, so the race
detector was not run locally and is not marked PASS. CI now runs
`go test -race ./internal/state ./internal/mux ./internal/control` on macOS for
every push to `main` and every pull request.

## Installed Relay check (no quota-consuming prompt)

The installer ran only after the control API reported `activeTurnCount = 0`
and `recoveryTaskCount = 0`. It staged and validated the current Store profile,
backed up the previous managed Relay app, replaced only the managed Relay copy,
repaired the launcher/shortcut, and reopened Relay. The official Store app was
not the installation target.

Sanitized post-install projection:

```json
{
  "health": true,
  "contractVersion": 1,
  "stateVersion": 2,
  "policy": "balanced",
  "effectivePolicy": "balanced",
  "handoffSupported": true,
  "activeTurnCount": 0,
  "recoveryTaskCount": 0,
  "connectedSubscriptions": 5,
  "availableSubscriptions": 2,
  "depletedSubscriptions": 3,
  "unknownSubscriptions": 0,
  "confirmedRemainingPercent": 66,
  "maximumPercent": 500,
  "routingCapacityOnly": true,
  "workerDiagnostics": 6,
  "nextCandidateIsPreview": true,
  "timelineRows": 10,
  "rawIdentityFieldsInRouterStatus": 0,
  "officialHomeReferencedByRelayAccounts": 0,
  "maximumDecisionLedgerLines": 2,
  "decisionRetentionBoundedAt": 100,
  "officialStoreAsarHashUnchanged": true,
  "officialStoreProcessesStillRunning": true,
  "relayProcessesRunning": true
}
```

The installed ASAR contains the 0.4.1 Routing Inspector markers: Routing
details, Why this account, explicit next preview, routing-capacity disclaimer,
session toast deduplication and the existing Usage surface. A source assertion
confirmed that the task badge has no fixed positioning. Startup compaction was
also observed on 40 existing per-thread decision ledgers; the largest retained
ledger contained 2 rows, below the 100-row contract bound.

## Evidence classification

- **Automated PASS:** yes, with the exit codes above.
- **Installed-app PASS (non-visual/read-only scope):** health, contract, isolated
  homes, process coexistence, installed assets, pool semantics and persisted
  state passed.
- **Installed visual interaction:** pending. The available Computer Use skill
  explicitly forbids automating ChatGPT/Codex UI, so no unsupported click-based
  claim is made for live panel placement, keyboard focus or Settings navigation.
- **LIVE quota E2E:** pending. No real prompt was sent, no reset credit was used,
  no account was signed out/removed, and no user task/history was changed.
